language: java
jdk: oraclejdk8
services: mongodb
sudo: required
addons:
  chrome: stable
before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - sleep 3
script: "mvn --quiet cobertura:cobertura"
after_success:
  - bash <(curl -s https://codecov.io/bash)
